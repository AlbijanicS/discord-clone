# Use exact global usernames for Friend Requests

Any authenticated User may send a Friend Request by supplying another User's exact globally unique username, without requiring a shared Workspace. The application deliberately provides neither fuzzy global search nor a browsable User directory, balancing connections outside Workspaces against unnecessary User discovery; Workspace member menus provide a second entry point using the same Friendships workflow.
